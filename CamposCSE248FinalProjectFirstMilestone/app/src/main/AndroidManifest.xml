<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.campos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".app.ViewCourseDetailsActivity"></activity>
        <activity android:name=".app.ViewSemesterDetailsActivity" />
        <activity android:name=".app.ViewScheduleDetailsActivity" />
        <activity android:name=".app.CreateSemesterActivity" />
        <!--
        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml:activity_main" />
        -->
        <activity android:name=".app.CreateScheduleActivity" />
        <activity android:name=".app.ViewScheduleActivity" />
        <activity
            android:name=".app.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>